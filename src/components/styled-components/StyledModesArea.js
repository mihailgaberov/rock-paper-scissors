import styled from 'styled-components'

const StyledModesArea = styled.div`
  text-align: center;
  
  label {
    font-size: .9em;
    text-transform: uppercase;
  }
`

export default StyledModesArea