import * as actionTypes from './actionTypes'

describe('Action Types Constants', () => {
  it('should have GET_SERVER_PICK', () => {
    expect(actionTypes.GET_SERVER_PICK).toEqual('GET_SERVER_PICK')
  })
})
